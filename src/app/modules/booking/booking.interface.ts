export type TBooking = {
  fullName: string;
  phone: string;
  carModel: string;
  date: string;
  time: string;
  slot: string;
  formula: string;
  expired: boolean;
};

export type TAdminCredential = {
  phone: string;
  email: string;
  password: string;
};
